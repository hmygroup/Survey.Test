<Window x:Class="SurveyApp.Views.Dialogs.QuestionaryDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="400"
        Width="600"
        MinHeight="300"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{Binding HeaderText}" 
                      FontSize="24" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,8" />
            <TextBlock Text="{Binding SubHeaderText}" 
                      FontSize="14" 
                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
        </StackPanel>

        <!-- Form Fields -->
        <StackPanel Grid.Row="2">
            <!-- Name Field -->
            <TextBlock Text="Name *" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,8" />
            <ui:TextBox Text="{Binding QuestionaryName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,16"
                    Height="32"
                    VerticalContentAlignment="Center"
                    PlaceholderText="Enter questionnaire name..."
                    MaxLength="200" />

            <!-- Description Field -->
            <TextBlock Text="Description" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,8" />
            <ui:TextBox Text="{Binding QuestionaryDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    Height="120"
                    PlaceholderText="Enter questionnaire description (optional)..."
                    MaxLength="1000" />

            <!-- Validation Message -->
            <TextBlock Text="{Binding ValidationMessage}"
                      Foreground="Red"
                      FontSize="12"
                      Margin="0,8,0,0"
                      Visibility="{Binding HasValidationError, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                   Content="Cancel"
                   IsCancel="True"
                   Click="CancelButton_Click"
                   Padding="24,8"
                   MinWidth="100"
                   Margin="0,0,8,0" />

            <ui:Button Grid.Column="2"
                   Content="{Binding SaveButtonText}"
                   IsDefault="True"
                   Click="SaveButton_Click"
                   Appearance="Primary"
                   Padding="24,8"
                   MinWidth="100"
                   IsEnabled="{Binding CanSave}" />
        </Grid>
    </Grid>
</Window>
