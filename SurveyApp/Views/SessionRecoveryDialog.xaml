<Window x:Class="SurveyApp.Views.SessionRecoveryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        
        Title="Recover Unfinished Survey"
        Width="500"
        Height="300"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🔄 Unfinished Survey Found"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>
            <TextBlock Text="You have an unfinished survey session. Would you like to continue where you left off?"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>

        <!-- Survey Info -->
        <Border Grid.Row="1"
                Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                Padding="15"
                CornerRadius="4"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Survey Details"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                
                <TextBlock x:Name="QuestionaryTitleText"
                           FontSize="16"
                           Margin="0,0,0,5"/>
                
                <TextBlock x:Name="LastSavedText"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                           Margin="0,0,0,5"/>
                
                <TextBlock x:Name="ProgressText"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
            </StackPanel>
        </Border>

        <!-- Message -->
        <TextBlock Grid.Row="2"
                   Text="Choose an action:"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            
            <Button x:Name="DiscardButton"
                    Content="Discard"
                    Click="DiscardButton_Click"
                    Width="100"
                    Margin="0,0,10,0"
                    ToolTip="Delete the saved session and start fresh"/>
            
            <Button x:Name="StartFreshButton"
                    Content="Start Fresh"
                    Click="StartFreshButton_Click"
                    Width="100"
                    Margin="0,0,10,0"
                    ToolTip="Keep the saved session but create a new one"/>
            
            <Button x:Name="ContinueButton"
                    Content="Continue"
                    Click="ContinueButton_Click"
                    Width="100"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTip="Resume from where you left off"/>
        </StackPanel>
    </Grid>
</Window>
